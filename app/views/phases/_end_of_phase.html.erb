<div id="is_end_<%= phase.id %>">
  <%= button_to "#{phase.is_end ? 'Sim' : 'Não'}", set_as_ending_phase_path(phase), method: :patch, form: { data: { turbo_frame: "is_end" } }, class: 'bg-blue-500 hover:bg-blue-600 text-white font-bold py-1 px-2 rounded' %>
</div>