<div id="is_end_<%= phase.id %>">
  <%= button_to "#{phase.is_end ? 'Sim' : 'Não'}", admin_set_as_ending_phase_path(phase), method: :patch, 
    class: 'bg-blue-500 hover:bg-blue-600 text-white font-bold py-1 px-2 rounded text-nowrap' 
  %>

  <%= button_to "Refresh", admin_refresh_phase_path(phase), method: :patch, 
    class: 'hidden' 
  %>
</div>
